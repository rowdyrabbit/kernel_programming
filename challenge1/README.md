To view the list of installed kernel modules:

`lsmod`

To install a compiled module:

`insmod <module_name.ko>`

To remove an installed module:

`rmmod <module_name>`

To view log output from the module:

`dmesg | tail`